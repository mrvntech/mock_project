import { useEffect } from "react"

export default function Cart() {
    useEffect(() => {
        document.addEventListener('click', (event) => {
            // if(event.)
        })
    }, [])
    return (
        <div className="cart">

        </div>
    )
}